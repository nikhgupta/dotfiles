# `~/.gitignore` is set as the global exclude file for `git`, via the git
# # configuration variables, which means that files in this configuration will
# always be ignored by `git` command, as if the following lines were specified
# within the project's `.gitignore` file.

# Special File #
################

# Common files #
################
*~
*.un~
*.sw[a-z]
*.rbc
.svn
.tissues
Session.vim

# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so
__pycache__/

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# Logs and databases #
######################
*.log
*.sql
*.sqlite

# OS generated files #
######################
._*
.DS_Store*
.Trashes
.Spotlight-V100
ehthumbs.db
Icon?
!Icon?/
Thumbs.db
Desktop.ini

# scm revert files
*.orig
# Netbeans project directory
/nbproject/
# RubyMine project files
.idea
# Textmate project files
/*.tmproj

# Ruby On Rails
.rake_tasks
zeus.json
custom_plan.rb
.pry_history

# miscelleneous/cache files
.sass-cache

# Tags: Ignore tags created by ctags, etags, etc.
.tags
!.tags/
TAGS
!TAGS/
tags
tags.lock
tags.te?mp
!tags/
gtags.files
GTAGS
GRTAGS
GPATH
cscope.files
cscope.out
cscope.in.out
cscope.po.out

# never ignore files that are named .notempty or .gitkeep
!.notempty
!.gitkeep
!.keep

# emacs per directory configuration
.dir-locals.el

# other directories
vendor/bundle
